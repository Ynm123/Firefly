# 个性化修改记录

> 本文档记录本博客从 Firefly 模板默认配置改为个人博客所做的所有修改，按序号递增记录，方便后续持续补充。

---

## 1. 修复站点 URL 指向旧域名

- **日期**：2026-08-26
- **文件**：`src/config/siteConfig.ts`
- **修改内容**：`site_url` 由 `https://firefly.cuteleaf.cn` 改为 `https://firefly-569.pages.dev`
- **原因**：博客实际部署在 Cloudflare Pages（`firefly-569.pages.dev`），原配置指向模板作者的域名，导致：
  - 社交分享图（og:image）指向无法访问的地址，分享时图片加载失败
  - 页面 canonical 链接、分享链接指向错误域名
- **影响**：og:image、canonical、分享链接均指向新的部署域名
- **验证**：✅ 已部署上线，og:image / og:url / canonical 均已指向 `https://firefly-569.pages.dev`

---

## 2. 重写「关于我」页面内容

- **日期**：2026-08-26
- **文件**：`src/content/spec/about.md`
- **修改内容**：将模板作者的自我介绍（夏叶）替换为博主本人的个人简介
  - 身份：网络空间安全专业学生（即将大四）
  - 博客定位：个人学习过程的记录载体，涵盖网络安全与网络工程两大方向（技术笔记 + 实验复盘）
  - 联系邮箱：`3264354544@qq.com`
  - 去除了原页面中的 Firefly/Fuwari 模板介绍、在线预览、开源地址等内容
- **影响**：`/about/` 页面展示博主个人简介
- **验证**：✅ 已部署上线，`/about/` 页面内容完整、邮箱正确显示，`pnpm check` 通过

---

## 3. 个性化个人资料配置（头像 / 名字 / 签名）

- **日期**：2026-08-26
- **文件**：`src/config/profileConfig.ts`、`src/assets/images/bird.jpg`（新增头像）
- **修改内容**：
  - 头像：`avatar.avif` → `bird.jpg`（新增图片，约 96KB）
  - 名字：`Firefly` → `Ynm`
  - 个人签名：`Hello, I'm Firefly.` → `Hello, I'm Ynm.`
  - 社交链接（QQ / GitHub / Email / RSS）全部注释掉，当前不显示
- **影响**：侧边栏个人资料组件显示新头像、名字 `Ynm` 及新签名
- **验证**：✅ `pnpm check` 通过；本机完整构建受 Google Fonts 网络限制未能完成（与本次修改无关，Cloudflare Pages 部署环境可正常访问）

---

## 4. 更新公告为安全提示 + 新增本地启动脚本

- **日期**：2026-08-26
- **文件**：`src/config/announcementConfig.ts`、`在本地快速启动博客.ps1`（新增）
- **修改内容**：
  - 公告内容：示例公告 → 安全提示「本博客仅供学习交流使用，请严格遵守法律法规，未经授权严禁对真实目标进行扫描或攻击！」
  - 公告不可关闭（`closable: false`）
  - 公告"了解更多"链接禁用（`link.enable: false`）
  - 新增 PowerShell 脚本 `在本地快速启动博客.ps1`：一键执行 `pnpm install` + `pnpm run dev` 启动本地开发服务器
- **影响**：侧边栏公告显示安全提示；本地可用脚本快速启动博客
- **验证**：✅ 已部署上线，公告显示安全提示且无"了解更多"链接，`pnpm check` 通过

---

## 5. 个性化背景壁纸配置

- **日期**：2026-08-26
- **文件**：`src/config/backgroundWallpaper.ts`
- **修改内容**：
  - 壁纸模式：`banner`（横幅）→ `fullscreen`（全屏）
  - 禁用背景视频播放（`playerEnable: false`，消除首页"视频加载失败"提示）
  - 桌面/移动壁纸：本地图片 → 远程随机壁纸 API（`https://wp.upx8.com/api.php`，nature/space 分类）
  - 文字雨特效展示暂停时间：2000ms → 3000ms
  - 首页标题下方链接图标：`linksEnable: false`（隐藏）
- **影响**：首页显示全屏远程随机壁纸，无背景视频
- **验证**：✅ 已部署上线，首页"视频加载失败"提示已消除，`pnpm check` 通过

---

## 6. 个性化站点标题/副标题 + 精简导航栏

- **日期**：2026-08-26
- **文件**：`src/config/siteConfig.ts`、`src/config/navBarConfig.ts`
- **修改内容**：
  - 站点标题：`Firefly` → `MyBLog`
  - 站点副标题：`Demo site` → `Ynm`
  - 注释掉导航栏"链接"子菜单（GitHub / Gitee / QQ交流群 / Firefly文档），消除模板作者相关导航
- **影响**：浏览器标签页/页面标题显示 "MyBLog - Ynm"；导航栏不再显示模板"链接"菜单
- **验证**：✅ 已部署上线，页面标题 "MyBLog - Ynm"，"链接"菜单已移除，`pnpm check` 通过

---

## 7. 侧边栏与站点细节个性化

- **日期**：2026-08-26
- **文件**：`src/config/sidebarConfig.ts`、`src/config/siteConfig.ts`
- **修改内容**：
  - 侧边栏布局：双侧边栏 → 仅左侧边栏（`position: "left"`）；文章页不再额外显示右侧边栏
  - 文章详情页隐藏左侧边栏个人资料、公告、音乐、分类、标签、动态等组件
  - 禁用"站点统计"与"站点信息"组件
  - 导航栏标题：`Firefly` → `MyBlog`
  - 主题色相 165 → 160；默认模式 `system` → `light`（亮色）；页面宽度 100 → 78（适配单侧边栏）
  - 卡片边框开启（`border: true`）；图片压缩质量 85 → 100
  - 站点开始日期改为 `2026-08-18`；隐藏文章"上次编辑时间"卡片；禁用分享海报
- **影响**：单侧边栏布局、默认亮色主题、导航栏显示 MyBlog
- **验证**：✅ 已部署上线，导航栏显示 MyBlog，仅左侧边栏，"站点统计/信息"已移除，`pnpm check` 通过

---

## 8. 清理模板示例内容（友链/相册/打赏/动态/书签导航）

- **日期**：2026-08-26
- **文件**：`src/config/friendsConfig.ts`、`src/config/galleryConfig.ts`、`src/config/sponsorConfig.ts`、`src/config/siteConfig.ts`、`src/content/dynamic/`
- **修改内容**：
  - 友链：禁用 3 个模板友链（Blog / Firefly Docs / Astro），关闭友链页底部自定义内容
  - 相册：注释掉 2 个示例相册（可爱流萤 / 加密相册示例）
  - 打赏：用途说明改为「你无需打赏」；关闭赞助者列表与文章内打赏按钮；禁用全部打赏方式（支付宝/微信/ko-fi/爱发电）
  - 书签导航页面关闭（`booknav: false`），导航栏移除"书签导航"菜单
  - 动态：删除 3 条模板示例动态，保留 1 条并更新发布日期为 2026-08-18（仍置顶）
- **影响**：友链/相册/打赏页面为空或提示；导航栏精简；侧边栏"最新动态"仅保留 1 条
- **验证**：✅ 已部署上线，导航栏移除"书签导航"，友链页模板友链已清除，`pnpm check` 通过

---

## 9. 首页横幅个性化 + 字体/壁纸/侧边栏细节

- **日期**：2026-08-26
- **文件**：`src/config/backgroundWallpaper.ts`、`src/config/fontConfig.ts`、`src/config/sidebarConfig.ts`、`src/config/siteConfig.ts`
- **修改内容**：
  - 首页横幅主标题：`Lovely firefly!` → `sudo universe --explore`
  - 首页横幅副标题：模板英文诗句 → 40 条极客风英文短语（Grep the stars / Ping 127.0.0.1 of the cosmos / Compiling dark matter... Done 等）
  - 壁纸模式：`fullscreen` → `banner`；模糊 10 → 20；卡片透明度 0.6 → 0.95
  - 横幅主标题字体：圆体（zen-maru-gothic）→ Inter
  - 侧边栏音乐/分类/标签组件位置：`sticky` → `top`（不随滚动）；开启动态组件标题显示
  - 文章提醒框（Admonitions）主题：`github` → `obsidian`（适配 Obsidian 来源文章）
- **影响**：首页横幅显示个性化极客文字，阅读体验更清新
- **验证**：✅ 已部署上线，横幅标题 `sudo universe --explore` 与极客副标题生效，`pnpm check` 通过
- **后续更新（2026-09-07）**：壁纸模式又由 `banner` 改回 `fullscreen`（全屏壁纸，`mode: "fullscreen"`），当前首页为全屏壁纸 + 无背景视频（`playerEnable: false`）

---

## 10. 接入 51la 访问统计与页脚计数器

- **日期**：2026-08-26
- **文件**：`src/config/analyticsConfig.ts`、`src/config/footerConfig.ts`、`src/config/FooterConfig.html`
- **修改内容**：
  - 启用 51la 统计（站点 ID：`3QuNK0eBBZS0LHtp`）
  - 关闭 51la 录屏功能（`screenRecord: false`）
  - 启用 Footer HTML 注入，在页脚添加 51la 访问计数器挂件
- **影响**：页脚显示访问计数器；站点接入访问统计
- **验证**：✅ 已部署上线，页脚 51la 计数器挂件与统计 ID 已生效，`pnpm check` 通过

---

## 11. 图片性能优化（压缩质量回退）

- **日期**：2026-08-26
- **文件**：`src/config/siteConfig.ts`
- **修改内容**：图片压缩质量 `quality: 100` → `85`（第 7 项曾改为 100，现回退到模板推荐区间 70-85）
- **原因**：文章截图为截图类 PNG，quality 100 体积比 85 大 30-50%，观感几乎无差别
- **附带排查**：首页"Image API Error" 为 CoverImage 组件隐藏错误占位（`hidden` class），`data-error="true"` 元素数为 0，浏览器实际不显示，无需修改
- **验证**：✅ 已部署上线，实测单图体积下降最多 46%（27.8KB → 15.1KB）

---

## 12. 文章页移除冗余信息（字数/阅读时间、许可协议卡片、相关文章）

- **日期**：2026-08-26
- **文件**：`src/pages/posts/[...slug].astro`（模板代码，作用于所有文章）
- **修改内容**（全部以注释形式保留，可随时恢复）：
  - 移除文章开头的"字数 / 阅读时间"（如"2634 字 13 分钟"）显示（含 overlay 封面模式下的同样信息）
  - 移除文章结尾的许可协议卡片（文章标题 / URL / 作者 / 发布于 / 许可协议）
  - 移除"相关文章 / 随机文章"推荐组件
  - 同步注释对应 import（`License` / `RecommendedPost` / `licenseConfig`）与 `relatedPosts` 变量
- **影响**：文章页更简洁，仅保留标题、元数据、正文、上一篇/下一篇、评论
- **验证**：✅ 已部署上线，Yone 页面已无字数/阅读时间、许可协议卡片、相关文章/随机文章，正文与图片完整，`pnpm check` 通过
- **后续更新（2026-09-06）**：上游 v6.16.7 合并后，本项描述已有部分变化——相关文章/随机文章/分享已改为 `siteConfig.post` 开关控制（默认 `false` 不显示，非代码移除），「上一篇/下一篇」导航已恢复显示（`postNavigation: true`）。详见第 14 项。

---

## 13. 文章详情页图片缩小至 90%

- **日期**：2026-08-26
- **文件**：`src/styles/markdown-extend.styl`、`src/styles/main.css`
- **修改内容**：
  - 正文图片（`.custom-md img`）：新增 `display: block; width: 90%; margin: 0 auto`（缩至 90% 并居中）
  - 封面图片（`#post-cover`）：新增 `width: 90%; margin: 0 auto`（缩至 90% 并居中）
- **备注**：使用 `width` 而非 `scale()`，避免模糊且不影响点击放大（fancybox）；`.image-grid`（`[grid]` 语法）文章未使用，无需修改
- **影响**：文章详情页封面与正文截图宽度 100% → 90%，水平居中
- **验证**：✅ 已部署上线，线上 CSS 已包含 `.custom-md img{width:90%}` 与 `#post-cover{width:90%}` 规则，`pnpm check` 通过

---

## 14. 同步上游 v6.16.7（合并 20 个 commit）

- **日期**：2026-09-06
- **文件**：全仓库（`git merge upstream/master`，上游 `0f2fab60` → `b53efac9`，38 文件 / +1737 -830）
- **修改内容**：
  - 模板版本 6.16.6 → **6.16.7**，同步上游 20 个提交的新功能与修复
  - 🆕 **沉浸阅读模式**：电脑端文章页右下角按钮，进入后只留「文章卡片 + 左侧目录」（新增 `ImmersiveReading.astro`、`ImmersiveTOC.astro`、`immersive-reading.css`、`immersiveReadingConfig.ts`、`immersive-reading-utils.ts`）
  - 🆕 **文章页底部按钮开关化**：原单一 `sharePoster` 拆分为 4 个独立开关 `share` / `postNavigation` / `relatedPosts` / `randomPosts`（相关/随机推荐组件同步升级，可分开控制）
  - 🆕 **SEO 元数据增强**：新增 `og:article` 结构化标签（发布时间/修改时间/作者/栏目/标签）、封面图补全 `alt` 与宽高；加密文章自动 `noindex, noarchive`
  - 🆕 归档标签/分类/未分类的**禁止访问规则**（可设 404）
  - 🎨 导航抽屉**点击链接后自动关闭** + 抽屉加宽与**毛玻璃效果**；设置面板/目录自定义滚动条样式
  - 🔧 移除不必要的 `will-change`（修复滚动残留）、简化壁纸切换逻辑、画廊路径检查修复、依赖升级
- **冲突处理**（仅 2 处，全部手动解决）：
  - `src/config/siteConfig.ts`：采纳上游新开关结构（冲突区域为旧的 `sharePoster: false` 被上游拆分）
  - `src/pages/posts/[...slug].astro`：相关/随机推荐区改为 **`siteConfig.post` 开关控制的条件渲染**（恢复 `RecommendedPost` / `getRelatedPosts` 等引用，实际显示与否由 config 决定，未来可一键开启）
- **个性化保留**：公告、侧边栏布局、标题/URL、图片 90%、许可证卡片/字数阅读时间移除（注释形式）等均无损保留
- **验证**：✅ `pnpm check`、`pnpm type-check`、`pnpm build` 全部通过，已推送 `7a1f1c7e`



---

## 15. 文章页配置收尾调整 + 公告内容换行修复

- **日期**：2026-09-06
- **文件**：`src/config/siteConfig.ts`、`src/components/widget/Announcement.astro`
- **修改内容**：
  - `postNavigation: false → true`：**恢复文章底部「上一篇/下一篇」导航显示**（上游合并后该功能开关化，原第 12 项移除此功能，现改为开启）
  - `immersiveReading.enable: true → false`：**隐藏沉浸阅读入口按钮**（新功能默认开启，个人不需要）
  - `share` / `relatedPosts` / `randomPosts`：维持 `false`（分享、相关/随机推荐不显示，仍可通过开关随时开启）
  - 公告内容换行修复：`Announcement.astro` 内容容器增加 `whitespace-pre-line`，使 `announcementConfig.content` 中的 `\n`/`\n\n` 正确显示为换行（此前 HTML 渲染会折叠换行符导致换行无效）
- **影响**：文章页恢复上一篇/下一篇导航、无分享/推荐/沉浸阅读按钮；公告可分两段显示（网络波动提示 + 安全提示）
- **验证**：✅ `pnpm check`、`pnpm type-check`、`pnpm build` 全部通过；dist 产物确认 `whitespace-pre-line` 生效，已推送
