---
title: ai提示词
published: 2026-08-19
draft: true
---

好的，基于你提供的 `Yone` 靶机博客文章，我为你提炼了一份通用的 **Vulnhub 靶机渗透测试 Write-Up 模板**。你可以将其作为未来编写类似博客的框架，只需替换 `[ ]` 中的内容即可。

```markdown
---
title: [靶机名称]
published: [YYYY-MM-DD]
description: [对 Vulnhub 靶机 [靶机名称] 的完整渗透测试记录，涵盖从信息收集到获取 root 权限的完整过程。]
image: ./images/[封面图片文件名].png
tags:
  - Vulnhub
  - 渗透测试
  - 靶机
  - 提权
category: Vulnhub
draft: false
pinned: false
lang: zh-CN
author: [你的名字/ID]
comment: true
---

| **项目**         | **详情**                                                                  |
| -------------- | ----------------------------------------------------------------------- |
| **靶机名称**       | [靶机名称]                                                                 |
| **难度等级**       | [简单/中等/困难]                                                              |
| **目标**         | [例如：获取 root 权限并找到 `/root` 目录下的 flag]                                  |
| **下载链接**       | [Vulnhub 下载页面链接]                                                       |
| **攻击机 (Kali)** | `[攻击机IP]`（建议使用 NAT 模式）                                               |
| **靶机**         | `[靶机IP]`（DHCP 自动分配 / 静态配置）                                         |

## 一、 信息收集 (Reconnaissance)

*信息收集是渗透测试的基石，决定了后续攻击面的广度。*

### 1.1 主机发现
使用 `nmap` 在本地网络中定位靶机 IP。
```
nmap -sn [本地网段，如 192.168.1.0/24]
```
![](images/[截图文件名].png)
结果输出：发现目标 IP 为：`[靶机IP]`

### 1.2 端口与服务扫描
使用 Nmap 对靶机进行全面的端口和服务扫描。

```
nmap -p- --min-rate 1000 [靶机IP]        # 快速全端口扫描
```
![](images/[截图文件名].png)
```
nmap -p [开放端口列表] -sV -sC -O [靶机IP]   # 深入服务探测
```
![](images/[截图文件名].png)

**扫描结果分析**：

| 端口 | 服务 | 状态 | 初步判断 |
| :--- | :--- | :--- | :--- |
| [端口] | [服务名] | 开放 | [例如：可能存在爆破或漏洞风险] |
| [端口] | [服务名] | 开放 | [例如：主要攻击面] |

## 二、 漏洞利用 (Exploitation)

*根据信息收集结果，选择最有可能的突破口进行深入利用。*

### 2.1 [漏洞类型/服务名称，如：Web 渗透 / SMB 服务]

#### 2.1.1 [具体步骤 1，如：绑定 Hosts 文件]
[描述操作步骤和目的]
```
[执行的命令]
```
![](images/[截图文件名].png)

#### 2.1.2 [具体步骤 2，如：发现路径遍历漏洞]
[描述操作步骤和目的]
```
[访问的 URL 或执行的命令]
```
![](images/[截图文件名].png)

**结果**：
[说明成功获取了什么关键信息，如用户名、密码、敏感文件等]

### 2.2 [获取初始 Shell]
*根据收集到的信息，尝试获取目标系统的低权限访问。*

#### 2.2.1 [攻击方法，如：SSH 密码爆破]
[描述操作步骤和目的]
```
[执行的命令]
```
**参数说明**：
- `[参数]`：[说明]
- `[参数]`：[说明]
![](images/[截图文件名].png)

**爆破结果**：
> 用户名：`[用户名]`
> 密码：`[密码]`

#### 2.2.2 连接 [服务，如：SSH]
使用获取到的凭据成功登录。
```
ssh [用户名]@[靶机IP]
```
查看当前的权限：
```
[用户名]@[主机名]:~$ id
uid=[UID]([用户名]) gid=[GID]([组名]) groups=[GID]([组名])
```
![](images/[截图文件名].png)

## 三、 权限提升 (Privilege Escalation)

*现在的目标是从普通用户提权至 `root`。*

### 3.1 检查 Sudo 权限
输入 `sudo -l` 查看当前用户能以 root 权限执行哪些命令。
```bash
sudo -l
```


**关键发现**：
[描述发现的 Sudo 配置，如：用户可以无密码地以 root 权限执行 `/usr/bin/restic backup -r rest*`]

### 3.2 [利用方法，如：利用 Restic 提权]
[描述利用思路，如：`restic` 是一个备份程序。如果我们可以控制 `restic` 的备份目标，就能利用它以 root 权限读取任意文件。]

#### 步骤 1：[在攻击机/云服务器上搭建服务]
[描述操作步骤和目的]
```
[执行的命令]
```


#### 步骤 2：[在靶机上执行操作]
[描述操作步骤和目的]
```
[执行的命令]
```


#### 步骤 3：[获取敏感信息]
[描述操作步骤和目的]
```
[执行的命令]
```


### 3.3 获取 Flag
[描述如何获取最终的 Flag 文件]
```
[执行的命令]
```


## 四、 获取 Root 密码（可选）

*如果还想进一步掌握 root 权限，可以尝试破解 root 密码。*

### 4.1 备份 Shadow 文件
[描述操作步骤和目的]
```
[执行的命令]
```


### 4.2 合并 Passwd 与 Shadow
[描述操作步骤和目的]
```
unshadow passwd.txt shadow.txt > hash.txt
```


编辑生成的文件，**只保留 `root` 和 `[已知用户名]` 用户**的相关行。

### 4.3 破解密码哈希
使用 Kali 自带的字典进行破解。
```
# 解压字典（如果需要）
sudo gunzip /usr/share/wordlists/[字典文件名].gz

john --wordlist=/usr/share/wordlists/[字典文件名].txt hash.txt
```

> **root 密码**：`[破解出的密码]`

### 4.4 切换 Root 权限
使用破解出的密码切换至 root 用户。
```bash
su root
```

成功获取 root 权限，靶机渗透完成。

## 五、 总结与复盘

### 5.1 攻击路径总结
1.  **信息收集**：[例如：通过端口扫描发现开放的服务]。
2.  **漏洞发现**：[例如：在 Web 应用中发现路径遍历漏洞，读取 `/etc/passwd`]。
3.  **获取 Shell**：[例如：利用读取到的用户名，通过 SSH 爆破获取初始访问权]。
4.  **权限提升**：[例如：通过 `sudo -l` 发现 `restic` 命令的滥用机会，利用备份功能读取 root 敏感文件]。

### 5.2 心得与思考
- **[心得点 1]**：[例如：一个小小的 `link` 参数泄露了 `/etc/passwd`，为后续 SSH 爆破提供了用户名依据，是整个渗透的突破口]。
- **[心得点 2]**：[例如：错误地配置 `sudoers` 文件，允许普通用户以 root 权限执行备份工具，是导致提权成功的根本原因]。
- **[心得点 3]**：[例如：`medusa` 在某些场景下比 `hydra` 更稳定流畅]。
```

---

### 💡 模板使用说明

| 模板部分 | 说明 |
| :--- | :--- |
| **Frontmatter** | 博客元数据，用于静态博客生成器（如 VitePress、Hexo 等）。按需调整。 |
| **环境信息表格** | 清晰列出靶机名称、难度、目标、下载链接和网络配置。 |
| **信息收集** | 包含主机发现和端口扫描，是所有渗透测试的标准起点。 |
| **漏洞利用** | 根据靶机特点灵活调整本节结构。可以按漏洞类型（如 SQL 注入、文件上传）或服务类型（如 Web、FTP）来划分小标题。 |
| **权限提升** | 重点描述从普通用户到 root 的提权过程，包括 `sudo` 配置检查、SUID 发现、内核漏洞利用等。 |
| **获取 Root 密码** | 此为可选章节，适合那些希望展示完整攻击链（包括密码破解）的博客。 |
| **总结与复盘** | 回顾攻击路径，并提炼有价值的心得体会，体现深度思考。 |

---

### 📝 博客写作建议

1. **截图规范**：将截图统一放在 `images/` 目录下，命名规范建议为 `Pasted%20image%20[日期时间].png`（与当前博客保持一致）。
2. **代码块语言标注**：在代码块后标注语言类型（如 `bash`、`plaintext`），提升可读性。
3. **关键步骤加粗**：对重要的操作步骤或发现用 **加粗** 强调。
4. **引用块使用**：对注意事项或解释性内容使用 `>` 引用块。
5. **表格应用**：对于端口扫描结果、参数说明等结构化信息，优先使用表格呈现。

---

这样，以后你再遇到新的 Vulnhub 靶机时，只需套用这个模板，替换对应内容，就能快速产出结构清晰、逻辑完整的渗透测试博客了。😊